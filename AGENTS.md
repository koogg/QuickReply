# AGENTS.md

本文件是 QuickReply 项目的维护规则。任何自动化代理或开发者在修改代码前都必须先阅读本文件，并同时查阅 `docs/` 下与任务相关的文档。

## 1. 项目背景

QuickReply 是一个 Windows 本地快捷回复桌面工具，用于管理分组话术、富文本、图片和文件附件，并通过全局快捷键、快速选择弹窗或吸附搜索，将内容粘贴到当前目标输入窗口。

项目是单机桌面应用，不是 Web 前后端系统。当前目标是稳定维护现有核心闭环，优先保证用户数据安全、Windows 兼容性和可回滚性。

## 2. 技术栈

- Python 3；当前开发虚拟环境为 CPython 3.14.6。
- PyQt6 桌面 GUI。
- pynput 全局键鼠监听。
- pywin32、winreg、ctypes Windows 系统集成。
- pypinyin 中文拼音搜索。
- JSON + 本地文件系统持久化，无数据库和 ORM。
- PyInstaller onedir Windows 打包。

依赖来源是根目录 `requirements.txt`。不要把当前虚拟环境中的偶然版本当作长期兼容承诺；修改依赖时必须说明兼容范围并验证打包。

## 3. 目录说明

- `main.py`：应用入口、工作目录、异常处理、单实例和 Qt 事件循环。
- `main_window.py`：主窗口、应用编排、分组和话术核心用例。
- `config.py`：路径和行为常量。
- `utils.py`：日志、资源路径、HTML 摘要、拼音与搜索工具。
- `listeners/`：全局键鼠监听。
- `widgets/`：PyQt 界面组件，以及现有数据管理、搜索和粘贴混入逻辑。
- `services/`：可测试的数据存储、schema 迁移、备份轮转和媒体路径服务。
- `tests/`：pytest 数据安全与搜索回归测试。
- `Date.json`：本地业务数据和用户设置。
- `media_files/`：运行时话术图片和附件。
- `backup/`：运行时 JSON 定时备份。
- `media_trash/`：未引用媒体的可恢复回收区。
- `build.py`、`QuickReply.spec`：PyInstaller 构建。
- `docs/`：项目接管、架构、数据、部署和开发计划文档。

## 4. 开发规范

### 通用规则

- 修改前先阅读相关调用链，确认 UI、内存状态、JSON 和 Windows 集成的影响。
- 保持改动最小且聚焦；不要把功能修改与无关格式化、重命名或大规模重构混在一起。
- 不得覆盖、丢弃或重置工作区中已有的用户修改。
- 保持代码和用户提示使用项目现有中文语境；标识符遵循 Python 命名习惯。
- 新模块应按“UI、应用服务、持久化、平台适配”明确职责，避免继续扩大 `main_window.py`。
- 日志统一使用 `utils.logger`；面向用户的可恢复错误使用 PyQt 消息框。
- 路径处理必须使用规范化路径并验证仍位于预期根目录，尤其是导入 JSON 和媒体文件操作。
- 与 Qt 控件有关的操作应在主线程执行；后台监听通过 Qt signal 与界面通信。
- 全局监听器、定时器和托盘资源必须有明确的启动、重配和退出清理路径。

### 数据兼容规则

- 未经明确的数据迁移方案，不得破坏现有 `Date.json` 顶层字段及话术字段。
- 新增持久化字段必须提供安全默认值，并兼容字段缺失的旧数据。
- 修改保存流程前必须考虑写入中断、原子性、备份和失败恢复。
- 搜索运行时缓存不得写入持久化文件。
- 附件引用应保存为 `media_files/` 下的相对路径，禁止持久化任意外部绝对路径。
- 涉及导入、删除、清理、迁移的功能必须先校验目标，并优先使用可恢复操作。

### 测试与验证规则

- 修复缺陷或新增逻辑时必须扩展现有 pytest 测试；使用 `requirements-dev.txt` 安装开发依赖，并通过 `python -m pytest` 运行。
- 纯逻辑至少覆盖正常、边界和异常输入。
- Windows 系统集成功能必须提供手工验证步骤，并在真实交互式 Windows 会话验证。
- 打包相关改动必须运行 `build.py`，并在无源码环境验证整个 `dist/QuickReply/` 目录。
- 不要用“能启动”代替数据导入、搜索、附件粘贴、托盘和退出等回归验证。

### 文档与提交规则

- 架构、数据格式、依赖、配置、运行方式变化时同步更新 `docs/`。
- 项目规则变化时更新本文件。
- 提交应说明用户影响、数据兼容性和验证结果。

## 5. 禁止修改目录和文件

除非用户明确要求且任务确有必要，否则禁止修改：

- `.git/`：版本库内部数据。
- `.venv/`、`venv/`、`env/`：本地虚拟环境。
- `.idea/`、`.vscode/`：个人 IDE 配置。
- `__pycache__/`：解释器缓存。
- `build/`、`dist/`：生成的构建产物；应由构建脚本重建，不手工编辑。
- `node_modules/`：如未来出现，属于生成依赖目录。
- `backup/`、`media_trash/`：用户备份和回收数据。
- `media_files/`：用户附件；测试不得使用真实用户媒体。
- `error.log`：运行日志，不作为源代码手工维护。
- `Date.json`：真实用户数据；除非任务明确要求数据迁移、初始化或示例数据变更，否则不得修改。
- `icon.ico`：品牌资源；除非任务明确是图标更新。

禁止把密钥、个人信息、真实客服话术、用户附件或生产数据提交到仓库。

## 6. 当前项目状态

- 核心功能已形成可用闭环：话术维护、搜索、快捷触发、粘贴、本地保存和备份。
- 当前定位仍是早期个人工具/原型，而非成熟企业产品。
- 仅支持 Windows，依赖系统剪贴板、窗口句柄和模拟按键。
- 数据仍使用单个 JSON，但已具备 schema v1、v0 兼容迁移、结构校验和原子写入；尚缺完整恢复快照与后续版本迁移。
- 已建立搜索、JSON、备份轮转、媒体路径、布局边界、混排粘贴和 Shell 窗口过滤 pytest 基线；仍无 CI/CD、安装器、代码签名或自动更新。
- `main_window.py` 同时承担较多 UI 与业务职责，维护耦合较高。
- 依赖只声明最低版本，构建可重复性不足。
- 原始 README 信息有限，详细现状以 `docs/` 为准。

## 7. 后续开发注意事项

优先级顺序：

1. 用户数据安全：完整备份包、恢复快照和后续 schema 迁移。
2. 回归保障：扩展单元测试、Windows 手工测试矩阵、CI 打包冒烟测试。
3. 安全边界：持续覆盖不可信导入、媒体重解析点和异常文件场景。
4. 架构解耦：逐步抽离 repository、媒体、搜索和 Windows 平台适配，不做无测试的一次性重写。
5. 体验改进与新功能：必须建立在前四项基础上，并由真实需求驱动。

特别注意：

- JSON 导出当前不包含媒体文件，不应把它描述为完整备份。
- 定时备份仅在应用运行时触发。
- 关闭主窗口是隐藏到托盘，不是退出。
- 目标应用对 HTML、图片、文件列表和模拟 Ctrl+V 的支持存在差异。
- 引入 SQLite、服务端、账号或同步前，应先确认真实业务需求和迁移成本。
- 任何可能修改或删除用户数据的动作，必须先备份、展示准确范围并提供恢复路径。

## 8. 维护入口

开始任务前至少阅读：

- `docs/项目概述.md`
- `docs/技术架构.md`
- 与任务相关的功能、数据或部署文档

如果代码事实与文档冲突，以实际代码和可复现验证为准，并在同一变更中修正文档。
